---
id: pr-cutlass-2161
repo: NVIDIA/cutlass
pr: 2161
title: Blockwise Improvement and Programmatic Dependent Launch
author: depaulmillz
date: 2025-03-10
url: https://github.com/NVIDIA/cutlass/pull/2161
source_category: upstream-code
architectures:
- sm100
tags:
- pdl
- gdc
- gemm
techniques:
- persistent-kernel
- tile-scheduling
hardware_features:
- tcgen05
- tmem
- clc
- gdc
- pdl
kernel_types:
- gemm
languages:
- cuda-cpp
captured_at: 2026-04-17
status: merged
merge_sha: 06e560d9
inclusion_reason: manually curated, Blackwell kernel relevance
changed_paths:
- include/cutlass/arch/grid_dependency_control.h
- include/cutlass/gemm/collective/sm100_mma_warpspecialized_blockwise_scaling.hpp
artifact_dir: store/corpus/artifacts/prs/cutlass/PR-2161
---

## Summary

Enables Programmatic Dependent Launch (PDL) via Grid Dependency Control (GDC) for SM100 Blackwell kernels, and fixes potential out-of-bounds memory access in blockwise/groupwise scaling copy operations. The PDL feature allows back-to-back kernel launches with hardware-managed dependencies, reducing launch overhead for persistent GEMM kernels.

## Problem

Two issues were addressed:

1. **Security/correctness**: The blockwise/groupwise scaling kernels used hardcoded copy sizes for scale factor arrays (SFA and SFB), which could lead to out-of-bounds memory access when tile dimensions did not match the hardcoded values.

2. **Performance**: SM100 Blackwell architecture supports Grid Dependency Control for programmatic dependent launch, but CUTLASS had not yet enabled this feature. Without GDC, back-to-back kernel launches on Blackwell could not overlap grid scheduling, leaving performance on the table for multi-kernel workloads.

## Solution / Techniques

### Grid Dependency Control for SM100

A new CMake option `CUTLASS_ENABLE_GDC_FOR_SM100` (default ON) enables GDC when compiling for SM100. The feature is gated behind compile-time architecture detection:

```cpp
#if (defined(CUTLASS_ENABLE_GDC_FOR_SM100) && \
     __CUDACC_VER_MAJOR__ >= 12 && \
     defined(__CUDA_ARCH__) && __CUDA_ARCH__ == 1000 && \
     defined(__CUDA_ARCH_FEAT_SM100_ALL))
```

### Blockwise Scaling Fix

Replaced hardcoded dimension values with computed `ElementsPerSFACopy` and `ElementsPerSFBCopy` constants derived from the tile shape, ensuring correct bounds for scale factor copy operations.

## Key Code

```cpp
// Grid Dependency Control enablement for SM100 (grid_dependency_control.h)
#if (defined(CUTLASS_ENABLE_GDC_FOR_SM100) && \
     __CUDACC_VER_MAJOR__ >= 12 && \
     defined(__CUDA_ARCH__) && __CUDA_ARCH__ == 1000 && \
     defined(__CUDA_ARCH_FEAT_SM100_ALL))
  #define CUTLASS_GDC_ENABLED
#endif

// Blockwise scaling fix: compute copy sizes from tile shape
static constexpr int ElementsPerSFACopy = ...;  // derived from CTA tile
static constexpr int ElementsPerSFBCopy = ...;  // derived from CTA tile
```

## Performance

PDL/GDC reduces kernel launch latency by allowing the GPU to overlap scheduling of dependent kernel grids. This is particularly beneficial for persistent GEMM kernels that use CLC-based tile scheduling on Blackwell, where back-to-back launches (e.g., GEMM followed by epilogue-fusion or another GEMM) can overlap without CPU synchronization. Performance improvements were validated through the CUTLASS gemm.
