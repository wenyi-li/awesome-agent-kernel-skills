---
id: pr-deepgemm-304
repo: deepseek-ai/DeepGEMM
pr: 304
title: '[Public release 26/04] Introducing Mega MoE, FP4 Indexer and other features/fixes'
author: LyricZhao
date: '2026-04-17'
url: https://github.com/deepseek-ai/DeepGEMM/pull/304
source_category: upstream-code
architectures:
- sm100
- sm90
tags:
- gemm
- moe
- fused-kernel
- nvfp4
- fp8
- fp4
- block-scale
- tcgen05
- tmem
techniques:
- kernel-fusion
- fine-grained-quantization
- communication-overlap
hardware_features:
- tcgen05
- tmem
- nvfp4
- fp4
- fp8
- block-scale
kernel_types:
- gemm
- moe
- grouped-gemm
- fused-kernel
languages:
- cuda-cpp
- ptx
captured_at: '2026-04-27'
status: merged
merge_sha: 7f2a703e
inclusion_reason: Major DeepGEMM public release with Mega MoE FP8xFP4 fused mega-kernel
  and FP4 Indexer; substantial SM100 kernel feature surface
changed_paths: []
artifact_dir: store/corpus/artifacts/prs/deepgemm/PR-304
---

## Summary

DeepGEMM's `[Public release 26/04]` ships the Mega MoE feature plus FP4 Indexer plus a batch of smaller features and fixes. The Mega MoE kernel fuses dispatch / linear-1 / SwiGLU / linear-2 / combine into a single mega-kernel that overlaps NVLink communication with tensor-core computation; only FP8 × FP4 MoE is supported in this release, and PyTorch ≥ 2.9 is required.

## Why this is the DeepGEMM full-ledger anchor for Round 4

This PR is the most substantive DeepGEMM change in the post-`2026-04-17` window: it crosses the kernel-feature boundary (Mega MoE adds a new SM100 fused MoE mega-kernel; FP4 Indexer is a new FP4 quantization path) rather than being a bugfix. It serves as the primary DeepGEMM upstream-code anchor for the wiki's `wiki/kernels/deepgemm.md` page in the Round 4 refresh.

## Changed Files

The full PR diff and 88 verbatim kernel source files (under `csrc/...`, `deep_gemm/...`) ship under [`store/corpus/artifacts/prs/deepgemm/PR-304/`](../../../../corpus/artifacts/prs/deepgemm/PR-304/). The fetch was performed in Round 5 R5-4 via `scripts/fetch_pr_diff.py`; the existing kernel-allowlist (`KERNEL_EXTS_C` for `.cu`/`.cuh`/`.hpp` and `KERNEL_PY_KEYWORDS` for `.py` paths matching `csrc`/`kernel`/`triton`/etc.) handled DeepGEMM's `csrc/...` layout without modification (no DeepGEMM-specific code-path needed). The `diff.patch` was truncated at the 1 MiB cap (full diff is ~1.07 MiB); `size_cap_truncated: true` is recorded in the bundle's `PROVENANCE.yaml`.

## Upstream Reference

- DeepGEMM PR #304: https://github.com/deepseek-ai/DeepGEMM/pull/304
- Merge commit: `7f2a703e`
- Author: LyricZhao
- Merged: 2026-04-17
