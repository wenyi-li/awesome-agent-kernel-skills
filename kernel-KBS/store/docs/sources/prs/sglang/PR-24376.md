---
id: pr-sglang-24376
repo: sgl-project/sglang
pr: 24376
title: Fix nixl mla key and backup skipping
author: hxieustc
date: '2026-05-04'
url: https://github.com/sgl-project/sglang/pull/24376
source_category: upstream-code
architectures:
- sm100
tags:
- attention
- mla
- python
techniques: []
hardware_features: []
kernel_types:
- attention
- mla
languages:
- python
captured_at: '2026-04-27'
status: merged
merge_sha: c3f9bc9818c935975995baa6fb1bbc26ffaffacf
inclusion_reason: 'matched keyword: attention'
changed_paths:
- python/sglang/srt/mem_cache/storage/nixl/README.md
- python/sglang/srt/mem_cache/storage/nixl/hicache_nixl.py
- python/sglang/srt/mem_cache/storage/nixl/test_hicache_nixl_storage.py
---

## Summary

# Motivation

Fix two bugs affecting MLA (Multi-head Latent Attention) models:

1. Fix incorrect MLA key denominator in batch_exists()

2. Missing MLA backup skip in NIXL backend.

# Modifications

1. In `hicache_nixl.py`, add early-return guard in `batch_set_v1()` and `batch_set()` when `backup_skip` is True, returning success without performing any storage operation.

2. Fix inverted MLA key denominator in batch_exists(): 

The zero-copy path used 1 if not self.is_mla_model else 2 to determine how many NIXL storage keys correspond to each logical page. This logic was inverted — MLA stores a single interleaved KV representation (only _k), so it should use denominator 1, while non-MLA (MHA) stores separate _k and _v entries and should use 2. The bug caused batch_exists() to return incorrect hit counts for MLA models.

Now it is fixed: `1 if self.is_mla_model else 2 (was 1 if not self.is_mla_model else 2)`

3. Updates on document:
- Document MLA write behavior: TP rank 0 writes; non-zero ranks skip locally in both `batch_set` and `batch_set_v1`
- Document zero-copy key expansion rules (MLA → _k only, MHA → _k + _v)
- Fix stale environment variable name (`SGLANG_

## Problem

Fix nixl mla key and backup skipping

## Changed Files

- `python/sglang/srt/mem_cache/storage/nixl/README.md`
- `python/sglang/srt/mem_cache/storage/nixl/hicache_nixl.py`
- `python/sglang/srt/mem_cache/storage/nixl/test_hicache_nixl_storage.py`
