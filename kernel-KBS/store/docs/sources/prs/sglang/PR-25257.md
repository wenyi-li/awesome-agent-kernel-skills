---
id: pr-sglang-25257
repo: sgl-project/sglang
pr: 25257
title: '[NPU] Support model DeepSeek-OCR and DeepSeek-OCR-2'
author: zhsurpass
date: '2026-05-14'
url: https://github.com/sgl-project/sglang/pull/25257
source_category: upstream-code
architectures:
- sm100
tags:
- attention
- cuda-cpp
- kernel-fusion
- moe
- python
- quantization
techniques:
- kernel-fusion
hardware_features: []
kernel_types:
- attention
- moe
- quantization
languages:
- cuda-cpp
- python
captured_at: '2026-04-27'
status: merged
merge_sha: b9ae8353d2af311a6484c96399966b59274c1bf5
inclusion_reason: 'matched keyword: cuda'
changed_paths:
- python/sglang/srt/models/deepseek.py
---

## Summary

## Motivation

Support DeepSeek-OCR and DeepSeek-OCR-2 model on NPU platform.

## Modifications

In NPU environment, `fused_moe_npu` from `sglang.srt.hardware_backend.npu.quantization.fused_moe_method_npu` should be used in `DeepseekMoE::forward()`.

## Accuracy Tests
Run the following command to start the SGLang inference service:
```
#!/bin/bash

export PYTHONPATH=/root/RemoteDev/sglang/python:$PYTHONPATH
export SGLANG_LOG_PATH=/root/run_scripts/DeepSeek-OCR-2/sglang.log
rm -rf $SGLANG_LOG_PATH

python -m sglang.launch_server \
--model-path  /home/weights/DeepSeek-OCR \
--enable-multimodal \
--mm-attention-backend ascend_attn \
--port 9090 \
--tp-size 1 \
--mem-fraction-static 0.7 \
--base-gpu-id 12 \
--max-total-tokens 4096 \
--attention-backend ascend \
--device npu \
--disable-cuda-graph \
--page-size 128 \
--skip-server-warmup
```
Run the following curl command to test the model performance:
```
root@localhost:/root/run_scripts/DeepSeek-OCR-2#curl -sS http://127.0.0.1:9090/v1/chat/completions \
  -H "Content-Type: application/json" \
  -d '{
    "model": "deepseek-ai/DeepSeek-OCR-2",
    "messages": [
      {
        "role": "user",

## Problem

[NPU] Support model DeepSeek-OCR and DeepSeek-OCR-2

## Changed Files

- `python/sglang/srt/models/deepseek.py`
