---
id: pr-sglang-24226
repo: sgl-project/sglang
pr: 24226
title: '[BugFix] fix(hicache): fix two slot-reuse races in DecodeKVCacheOffloadManager'
author: kflansburg
date: '2026-05-01'
url: https://github.com/sgl-project/sglang/pull/24226
source_category: upstream-code
architectures:
- sm100
tags:
- decode
- prefill
- python
- register-budgeting
techniques:
- register-budgeting
hardware_features: []
kernel_types:
- decode
- prefill
languages:
- python
captured_at: '2026-05-23'
status: merged
merge_sha: 643d44d699285fe21e6cd347864277b72a358dba
inclusion_reason: 'matched keyword: mma'
changed_paths:
- python/sglang/srt/disaggregation/decode_kvcache_offload_manager.py
- test/registered/disaggregation/test_disaggregation_decode_offload.py
- test/registered/disaggregation/test_specv2_kvcache_offloading.py
---

## Summary

## Motivation
`DecodeKVCacheOffloadManager` had two related slot-reuse races. 

1. `offload_kv_cache` called `token_to_kv_pool_allocator.free(token_indices[:state.prefill_len])` on the first incremental offload of each request (after `offload_stride` = `page_size` decode tokens). The request continues to decode and attend to those slot indices via `req_to_token_pool.req_to_token`, so reusing them by a concurrent new request's prefill produces cross-pollinated KV reads. Symptom: ~5% of responses collapse mid-decode into a degenerate fixed-point loop of literal substrings from a peer request's prompt under sustained load. 
2. even once the request is finished, the `cache_controller` may still have outstanding async device→host KV copies in flight; freeing those slots before the copies land lets a new prefill overwrite KV that the offload DMA is still reading, corrupting the bytes that end up in the host pool.

## Modifications
Move all prefill-aligned slot freeing into `_release_finished_req`, which only runs once the request is finished. Track outstanding offload writes per request via an `offload_inflight` counter incremented in `offload_kv_cache` and decremented as each ack

## Problem

[BugFix] fix(hicache): fix two slot-reuse races in DecodeKVCacheOffloadManager

## Changed Files

- `python/sglang/srt/disaggregation/decode_kvcache_offload_manager.py`
- `test/registered/disaggregation/test_disaggregation_decode_offload.py`
- `test/registered/disaggregation/test_specv2_kvcache_offloading.py`
