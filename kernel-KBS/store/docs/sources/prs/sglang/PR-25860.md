---
id: pr-sglang-25860
repo: sgl-project/sglang
pr: 25860
title: add git gemm warpper for dispatch_bf16_fp32_backend
author: BJWang-ant
date: '2026-05-20'
url: https://github.com/sgl-project/sglang/pull/25860
source_category: upstream-code
architectures:
- sm100
tags:
- attention
- cache-policy
- gemm
- moe
- nvfp4
- prefill
- python
- vectorized-loads
techniques:
- cache-policy
- vectorized-loads
hardware_features:
- nvfp4
kernel_types:
- attention
- gemm
- moe
- prefill
languages:
- python
captured_at: '2026-05-23'
status: merged
merge_sha: ce7141ef98767f756ccc949f5716f3633afad01e
inclusion_reason: matched kernel path
changed_paths:
- python/sglang/jit_kernel/deepseek_v4.py
---

## Summary

## Motivation
We added a JIT DeepGemm code branch for _dispatch_bf16_fp32_backend to reduce cache generation during the runtime stage.

## Perf Test
server command：
```
SGLANG_SET_CPU_AFFINITY=1 \
  SGLANG_OPT_SWA_SPLIT_LEAF_ON_INSERT=1 \
  SGLANG_OPT_SWA_EVICT_DROP_PAGE_MARGIN=1 \
  SGLANG_OPT_FIX_MEGA_MOE_MEMORY=1 \
  SGLANG_DEEPEP_NUM_MAX_DISPATCH_TOKENS_PER_RANK=0 \
  SGLANG_OPT_SWA_RELEASE_LEAF_LOCK_AFTER_WINDOW=1 \
  SGLANG_OPT_DEEPGEMM_MEGA_MOE_NUM_MAX_TOKENS_PER_RANK=8320 \
  SGLANG_OPT_DEEPGEMM_MEGA_MOE_USE_FP4_ACTS=1 \
  SGLANG_OPT_DEEPGEMM_MEGA_MOE_USE_MXF4_KIND=1 \
  sglang serve \
  --trust-remote-code \
  --model-path /deepseek-ai/DeepSeek-V4-Pro \
  --tp 8 \
  --dp 8 \
  --enable-dp-attention \
  --moe-a2a-backend megamoe \
  --mem-fraction-static 0.87 \
  --max-running-requests 512 \
  --swa-full-tokens-ratio 0.075 \
  --chunked-prefill-size 65536 \
  --tokenizer-worker-num 8 \
  --enable-prefill-delayer \
  --speculative-algo EAGLE \
  --speculative-num-steps 3 \
  --speculative-eagle-topk 1 \
  --speculative-num-draft-tokens 4 \
  --deepep-config '{"normal_dispatch":{"num_sms":96},"normal_combine":{"num_sms":96}}' \
  --host 127.0.

## Problem

add git gemm warpper for dispatch_bf16_fp32_backend

## Changed Files

- `python/sglang/jit_kernel/deepseek_v4.py`
