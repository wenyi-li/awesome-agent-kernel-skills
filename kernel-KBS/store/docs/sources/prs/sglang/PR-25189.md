---
id: pr-sglang-25189
repo: sgl-project/sglang
pr: 25189
title: '[perf] DeepSeekV3: drop redundant FP32 upcasts in trtllm MoE paths'
author: maocheng23
date: '2026-05-13'
url: https://github.com/sgl-project/sglang/pull/25189
source_category: upstream-code
architectures:
- sm90
- sm100
tags:
- cute-dsl
- fine-grained-quantization
- fp8
- gemm
- kernel-fusion
- moe
- nvfp4
- python
- quantization
- tcgen05
- triton
techniques:
- fine-grained-quantization
- kernel-fusion
hardware_features:
- fp8
- nvfp4
- tcgen05
kernel_types:
- gemm
- moe
- quantization
languages:
- cute-dsl
- python
- triton
captured_at: '2026-04-27'
status: merged
merge_sha: 2df9e8b4b33112eed19d707b4e51e5a90ac39ce8
inclusion_reason: matched kernel path
changed_paths:
- python/sglang/srt/layers/moe/moe_runner/flashinfer_trtllm.py
- python/sglang/srt/models/deepseek_v2.py
---

## Summary

## Summary

Drop the explicit `.to(torch.float32)` upcasts on `router_logits` before each `trtllm_*_moe` call for DeepSeekV3 routing. flashinfer ≥ 0.6.8 (`trtllm_fp4_block_scale_moe`, `trtllm_fp8_block_scale_moe`, `trtllm_fp8_per_tensor_scale_moe`) accept BF16 `router_logits` directly, so the explicit FP32 upcasts are no longer required.

flashinfer is pinned at `0.6.11.post1` in `python/pyproject.toml`, well past the 0.6.8 cutoff.

## Changes

**`python/sglang/srt/layers/moe/moe_runner/flashinfer_trtllm.py` — three sites**

1. FP4 block-scale path (`fused_experts_none_to_flashinfer_trtllm_fp4`): drop the `if routing_method_type == DeepSeekV3: router_logits = router_logits.to(torch.float32)` block.
2. FP8 block-scale path (`trtllm_fp8_block_scale_moe_wrapper` call): drop the inline ternary `.to(torch.float32) if DSV3 else router_logits`.
3. FP8 per-tensor path (`trtllm_fp8_per_tensor_scale_moe_wrapper` call): simplify the FP32/BF16 conditional to an unconditional `.to(torch.bfloat16)`.

**`python/sglang/srt/models/deepseek_v2.py`**

`MoEGate.forward` continues to emit FP32 router_logits. A `TODO: will check the dtype to be bf16` is added to the SM100/103 + 256-expert fast path as a

## Problem

[perf] DeepSeekV3: drop redundant FP32 upcasts in trtllm MoE paths

## Changed Files

- `python/sglang/srt/layers/moe/moe_runner/flashinfer_trtllm.py`
- `python/sglang/srt/models/deepseek_v2.py`
