---
id: pr-flash-attention-2550
repo: Dao-AILab/flash-attention
pr: 2550
title: fix varlen w/ paging split kv bug
author: liangel-02
date: '2026-05-09'
url: https://github.com/Dao-AILab/flash-attention/pull/2550
source_category: upstream-code
architectures:
- sm100
tags:
- attention
- cuda-cpp
- flash-attention
- python
techniques: []
hardware_features: []
kernel_types:
- attention
- flash-attention
languages:
- cuda-cpp
- python
captured_at: '2026-05-23'
status: merged
merge_sha: 9bad4bec7326ad28edb5516b8878fd283f8991c0
inclusion_reason: matched kernel path
changed_paths:
- csrc/flash_attn/flash_api.cpp
- tests/test_flash_attn.py
---

## Summary

Paged KV always runs with split KV kernel because mha_varlen_fwd calls run_mha_fwd(..., force_split_kernel = paged KV) but because set_params_splitkv was never called before #[2448](https://github.com/Dao-AILab/flash-attention/pull/2448), num_splits stayed 0 → the splitting heuristic never ran so even though the split KV kernel always runs with paged KV, it still did only a single pass.

Before #[2448](https://github.com/Dao-AILab/flash-attention/pull/2448), set_params_splitkv (which sets the oaccum + runs the heuristic) only ran when seqlen_ngroup_swapped = True, which also reshapes the varlen input to have a batch dimension.

Today, the condition for calling set_params_splitkv is seqlen_ngroup_swapped = True OR paged because we wanted to give users the option to pass in num_splits=1 as a way to force batch invariance between paged (split kernel) vs not paged (regular kernel), but then this caused the heuristic to run, potentially setting num_splits > 1. So then now the split KV kernel could actually run with real splitting which causes the varlen o_batch_stride bug (see https://github.com/Dao-AILab/flash-attention/pull/2542)

This PR fixes the bug by restoring original beha

## Problem

fix varlen w/ paging split kv bug

## Changed Files

- `csrc/flash_attn/flash_api.cpp`
- `tests/test_flash_attn.py`
