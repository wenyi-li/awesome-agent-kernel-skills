---
id: pr-tensorrt-llm-14219
repo: NVIDIA/TensorRT-LLM
pr: 14219
title: '[None][feat] DSv4: enable GVR Heuristic Top-K for compress_ratio=4'
author: longcheng-nv
date: '2026-05-17'
url: https://github.com/NVIDIA/TensorRT-LLM/pull/14219
source_category: upstream-code
architectures:
- sm100
tags:
- attention
- cuda-cpp
- decode
- mla
- prefill
- python
- register-budgeting
- sparse-attention
- tcgen05
- vectorized-loads
techniques:
- register-budgeting
- vectorized-loads
hardware_features:
- tcgen05
kernel_types:
- attention
- decode
- mla
- prefill
- sparse-attention
languages:
- cuda-cpp
- python
captured_at: '2026-05-23'
status: merged
merge_sha: bef645d171db4b3920d6e26b23dd353379b53d8b
inclusion_reason: matched kernel path
changed_paths:
- cpp/tensorrt_llm/kernels/heuristicTopKDecode.cu
- cpp/tensorrt_llm/kernels/heuristicTopKDecode.h
- cpp/tensorrt_llm/kernels/indexerTopK.cu
- tensorrt_llm/_torch/attention_backend/sparse/dsa.py
- tensorrt_llm/_torch/model_config.py
- tensorrt_llm/llmapi/llm_args.py
- tests/unittest/_torch/thop/parallel/test_indexer_topk.py
---

## Summary

## Summary

- Widens the `canUseHeuristic` gate in `cpp/tensorrt_llm/kernels/indexerTopK.cu` from `compressRatio == 1` to `compressRatio ∈ {1, 4}`, enabling GVR Heuristic Top-K on the DSv4 indexer (which operates in compressed-token-index space).
- Threads `compressRatio` through `launchHeuristicTopKDecode` (3 dtype overloads). Kernel forces `preIdxOffset = 0` when `cr != 1` (compressed entries are appended at the end, so prev-step indices in `[0, c_prev-1]` remain valid in `[0, c_curr-1]`), and divides `seqLens` by `compressRatio` internally to derive per-row compressed `N`.
- Adds pass-through for 6 `DeepSeekSparseAttentionConfig` sibling fields in `tensorrt_llm/_torch/model_config.py:681-731` so user-set `enable_heuristic_topk: true` (and friends) survive the V4 `sparse_attention_config` rebuild. Previously the V4 rebuild silently dropped these back to subclass defaults — a user setting `enable_heuristic_topk: true` in `--extra_llm_api_options` had no effect on V4.
- Adds 108-case parametrized correctness test `test_indexer_topk_decode_dist_v4_cr4` in `tests/unittest/_torch/thop/parallel/test_indexer_topk.py` (cr=4 × MTP {1,2,3} × K {512,1024,2048} × {fp32,bf16,fp16} × batch_siz

## Problem

[None][feat] DSv4: enable GVR Heuristic Top-K for compress_ratio=4

## Changed Files

- `cpp/tensorrt_llm/kernels/heuristicTopKDecode.cu`
- `cpp/tensorrt_llm/kernels/heuristicTopKDecode.h`
- `cpp/tensorrt_llm/kernels/indexerTopK.cu`
- `tensorrt_llm/_torch/attention_backend/sparse/dsa.py`
- `tensorrt_llm/_torch/model_config.py`
- `tensorrt_llm/llmapi/llm_args.py`
- `tests/unittest/_torch/thop/parallel/test_indexer_topk.py`
