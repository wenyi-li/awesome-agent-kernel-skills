---
id: pr-flashinfer-3292
repo: flashinfer-ai/flashinfer
pr: 3292
title: 'bench(moe_deepseek): fix moe benchmark (supersedes #2886)'
author: leejnau
date: '2026-05-11'
url: https://github.com/flashinfer-ai/flashinfer/pull/3292
source_category: upstream-code
architectures:
- sm100
tags:
- cute-dsl
- decode
- kernel-fusion
- moe
- python
- tcgen05
techniques:
- kernel-fusion
hardware_features:
- tcgen05
kernel_types:
- decode
- moe
languages:
- cute-dsl
- python
captured_at: '2026-05-23'
status: merged
merge_sha: 4381afc1fd1394db2873d7420f8f364e4f8c8cb3
inclusion_reason: matched kernel path
changed_paths:
- benchmarks/bench_moe_deepseek.py
---

## Summary

## 📌 Description

This refreshes `benchmarks/bench_moe_deepseek.py` so it runs cleanly on current main. It rebases Yunzhe Qiu's bench rewrite (`5677a080` from #2886, which restructures the bench so autotune runs inside the `bench_gpu_time` measurement region) onto post-#3252 main, plus a small follow-up that fixes a stale `RoutingMethodType` import.

Two commits from the original #2886 are intentionally dropped because their fixes have since landed independently. `c0b80b64`'s `num_tokens <= max_num_tokens` prealloc guard is now subsumed by #3252 — the `use_prealloc` predicate in `cute_dsl/fused_moe.py` already includes that check. And `f3beb602`'s `_force_autotune_off()` bench-side workaround for CUPTI measurement pollution is no longer needed: #3126 moved the cache lookup ahead of `_prepare_input_tensors` synthesis in the autotuner's tuning-mode loop, eliminating the pollution at the source. The only remaining mismatch with current main was `RoutingMethodType`, which moved from `flashinfer.fused_moe.core` to `flashinfer.tllm_enums` (and is re-exported via `flashinfer.fused_moe`) — fixed in the second commit here.

Verified on B200 inside `nvcr.io/nvidia/tensorrt-llm/release:

## Problem

bench(moe_deepseek): fix moe benchmark (supersedes #2886)

## Changed Files

- `benchmarks/bench_moe_deepseek.py`
