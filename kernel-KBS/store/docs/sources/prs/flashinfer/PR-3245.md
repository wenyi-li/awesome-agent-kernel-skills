---
id: pr-flashinfer-3245
repo: flashinfer-ai/flashinfer
pr: 3245
title: bump version to 0.6.11
author: aleozlx
date: '2026-05-06'
url: https://github.com/flashinfer-ai/flashinfer/pull/3245
source_category: upstream-code
architectures:
- sm90
tags:
- attention
- decode
- fine-grained-quantization
- fp8
- gemm
- grouped-gemm
- kernel-fusion
- moe
- nvfp4
- python
- quantization
- tcgen05
- tma
techniques:
- fine-grained-quantization
- kernel-fusion
hardware_features:
- fp8
- nvfp4
- tcgen05
- tma
kernel_types:
- attention
- decode
- gemm
- grouped-gemm
- moe
- quantization
languages:
- python
captured_at: '2026-05-23'
status: merged
merge_sha: cb44e7df429cee12330e799ca91e510b9eae04f6
inclusion_reason: 'matched keyword: kernel'
changed_paths:
- version.txt
---

## Summary

## Description

Bump version to 0.6.11 for release.

## Related Issues (Gated-by PRs)

https://github.com/flashinfer-ai/flashinfer/issues?q=is%3Aopen+label%3Av0.6.11

## Reviewer Notes

**API changes review**

API changes since v0.6.10

```diff
$ git diff v0.6.10..main -- "*.py" | grep -B5 -A20 "@flashinfer_api"
+# the returned tensor, any caller-side ``view`` / ``slice`` / ``clone`` that
+# drops the original tensor would silently free the workspace under the kernel.
+_workspace_keepalive: Dict[int, MnnvlMemory] = {}
+
+
 @flashinfer_api
 def decode_cp_a2a_workspace_size(cp_size: int) -> int:
     """Return the workspace size **in bytes** per rank for the given CP group size.
@@ -119,33 +133,24 @@ def decode_cp_a2a_workspace_size(cp_size: int) -> int:
 
 
 @flashinfer_api
-def decode_cp_a2a_allocate_workspace(
-    cp_size: int,
-    cp_rank: int,
+def decode_cp_a2a_allocate_mnnvl_workspace(
+    mapping: Mapping,
     *,
-    mapping: Optional[Mapping] = None,
     mnnvl_config: Optional[MnnvlConfig] = None,
 ) -> torch.Tensor:
-    """Allocate a workspace tensor of shape ``[cp_size, ws_elems_per_rank]``.
+    """Allocate an MNNVL-backed workspace of shape ``[cp_size, ws_elems_pe

## Problem

bump version to 0.6.11

## Changed Files

- `version.txt`
