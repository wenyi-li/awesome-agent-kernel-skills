# Workflow 详细操作指南

## Phase 0: Name Lookup

```bash
python <SCRIPTS_DIR>/operator_registry.py lookup <op>
```

输出示例：
```
算子名:     _cholesky_solve_helper
规范名:     _cholesky_solve_helper
加速比:     15.1576
PR链接:     (未提交)
规范名表行: 120
```

规范名影响：yaml `id`、pytest mark、PR title、benchmark `op_name`（均使用去掉前导下划线的版本）。
大部分 aten 算子规范名和原名相同，仅少数 fused 算子需转 PascalCase。

## Phase 1: Preparation

```bash
cd $FLAGGEMS_REPO
git checkout -b pr/<op> upstream/master
```

确认不存在于上游：
```bash
git show upstream/master:src/flag_gems/ops/<op>.py  # should fail
```

## Phase 1.5: Benchmark & Data

```bash
# 完整模式：运行 benchmark + 查国产卡
CUDA_VISIBLE_DEVICES=<N> python .claude/skills/flaggems-pr-submit/scripts/gen_pr_description.py <op> \
  --repo $FLAGGEMS_REPO

# 仅查国产卡
python .../gen_pr_description.py <op> --skip-run

# 从 CI 日志 pipe
python -m pytest benchmark/test_<op>.py --level core -s | python .../gen_pr_description.py <op> --nvidia-stdin
```

## Phase 2: Code Preparation (6 files)

### 2.1 Kernel: `src/flag_gems/ops/<op>.py`
- 从 `.worktrees/gen-<op>/src/flag_gems/ops/<op>.py` 复制
- 确保首行 `# Generated by KernelGen: https://github.com/flagos-ai/KernelGen`
- Review: logging（非 print）、无重复函数、无多余 import torch、无意义封装删除、hardcode size 加注释

### 2.2 Register: `src/flag_gems/ops/__init__.py`
- 按字母序加 import + `__all__` 条目（下划线前缀模块排前面）

### 2.3 Register: `src/flag_gems/__init__.py`
- 按字母序加 `_FULL_CONFIG` 条目：`("<aten_op>", <func>)`
- overload: `("<op>.Tensor", <func>)`; inplace: `("<op>_", <func>_)`

### 2.4 Test: `tests/test_<op>.py`
**CRITICAL: 从 worktree 提取，不允许重写测试逻辑。**

1. 找到 worktree 测试：`grep -rn "def test_accuracy_<op>" .worktrees/gen-<op>/tests/`
2. 提取完整函数（含装饰器、比较逻辑、atol）
3. 仅做以下改动：
   - import 改为 `from . import accuracy_utils as utils`
   - 函数名 `test_accuracy_<op>` → `test_<op>`
   - 去掉 vendor-specific 分支
4. 不改变：比较逻辑、atol 值、参数化方式、输入构造

如 worktree 无测试，使用模板：
```python
import pytest
import torch
import flag_gems
from . import accuracy_utils as utils

@pytest.mark.<op>
@pytest.mark.parametrize("shape", utils.POINTWISE_SHAPES)
@pytest.mark.parametrize("dtype", utils.FLOAT_DTYPES)
def test_<op>(shape, dtype):
    inp = torch.randn(shape, dtype=dtype, device=flag_gems.device)
    ref_inp = utils.to_reference(inp)
    ref_out = torch.<op>(ref_inp)
    with flag_gems.use_gems():
        res_out = torch.<op>(inp)
    utils.gems_assert_close(res_out, ref_out, dtype)
```

关键约束：
- 相对导入：`from . import accuracy_utils as utils`
- mark 与算子 ID 一致（下划线前缀算子去掉下划线）
- 用 `to_reference` 获取参考（所有参与参考计算的 tensor 都要转）
- `gems_assert_close` 只支持 atol，不支持 rtol
- 禁止 print()

### 2.5 Benchmark: `benchmark/test_<op>.py`

选择 benchmark class：
| 类型 | Class |
|------|-------|
| 一元 pointwise | `base.UnaryPointwiseBenchmark` |
| 二元 pointwise | `base.BinaryPointwiseBenchmark` |
| Reduction | `base.UnaryReductionBenchmark` |
| BLAS | `base.BlasBenchmark` |
| 自定义输入 | 继承 `base.GenericBenchmark`，传入 `input_fn` 或覆盖 `get_input_iter` |

模板：
```python
import pytest
import torch
from . import base, consts

@pytest.mark.<op>
def test_<op>():
    bench = base.UnaryPointwiseBenchmark(
        op_name="<op>",
        torch_op=torch.<op>,
        dtypes=consts.FLOAT_DTYPES,
    )
    bench.run()
```

### 2.6 YAML: `conf/operators.yaml`
按 `id` 字母序插入：
```yaml
- id: <op>
  description: |
    <one-line from PyTorch docs>
  for:
    - <aten_op_name>
  labels:
    - aten
    - KernelGen
    - pointwise  # or reduction, etc.
  kind:
    - Math  # or Reduction/NeuralNetwork/LinearAlg/Tensor/Logic
  stages:
    - alpha: '5.1'
```

## Phase 3-4: Validation & Pre-commit

```bash
# Phase 3
python scripts/check_operator.py <op> --repo-dir $FLAGGEMS_REPO

# Phase 4
pre-commit run --files src/flag_gems/ops/<op>.py tests/test_<op>.py benchmark/test_<op>.py \
  src/flag_gems/ops/__init__.py src/flag_gems/__init__.py conf/operators.yaml
```
自动修复后 re-stage，循环直到全过。F401 需手动删多余 import。

## Phase 5: Git Rules & Push

- **NEVER `git add -A` or `git add .`** — 687 worktrees 会被误加
- **NEVER Co-Authored-By** — CLA CI 会失败
- 只 stage 6 个文件，分支名 `pr/<op>`，push 到 `origin`（fork）

## Phase 6: Backfill

```bash
python scripts/operator_registry.py backfill <op> <pr_url>

# 查看状态
python scripts/operator_registry.py pending --limit 20
python scripts/operator_registry.py submitted
```

## Code Review Focus Points

1. 重复函数定义 2. 多余 import torch 3. print() 4. 相对导入
5. 三处注册完整性 6. 字母序 7. mark 一致 8. benchmark class 选择
9. snake_case 命名 10. 死代码删除 11. Triton 实现 12. @use_tl_extra 桩函数

## Batch Submission

- 每批 10-20 个 PR，gh API 调用间隔 5 秒
- 优先 pointwise（简单，review 快）
- 记录每个算子 success/failure
